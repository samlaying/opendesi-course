# MVP 交付清单

通过标准针对**这个要做出来的产品**（FitLog），不是「OpenPencil 能不能打开」。

| 项 | 通过标准 | 现状 | 证据 |
|---|---|---|---|
| 主路径可点 | 打开 → 记一笔（部位/动作/重量×组数）→ 打卡成功 streak+1 | ✅ 通过 | Playwright e2e：保存后 `已打卡 streak=1`，5/5 断言绿（2026-09-12） |
| 状态真实 | 空 / 加载 / 失败出现在主路径 | ✅ 通过 | 空态实测「还没有记录+记第一笔」；本地应用无网络加载态（以数据初始化态代替，DS 规则3）；表单校验失败实测拦截（空组数 → 字段下错误文案） |
| 规则进界面 | DS 的禁用/权限有体现 | ✅ 通过 | 主按钮每屏唯一（今日=记一笔、表单=保存）；打卡绿只用于状态（✓/点阵/补角标），按钮始终橙；清空=二次确认+非 primary；未来日期禁点（nextDay/nextMon disabled）；streak 由数据计算非硬编码 |
| 代码来源 | 写明 Agent / 人各占哪段 | ✅ 已写明 | Agent 100% 起草（Claude，按 Context+DS 生成）；人未手改代码，仅口头授权「后续全部完成」。人手改段：无（首次交付） |
| 部署 | URL 或本地启动命令 | ✅ 可用 | `open 项目/项目1/app/index.html`（纯静态单文件，file:// 直开即用，无需服务器；离线可用） |
| 验证 | 手工走一遍或 1 个日志 | ✅ 已验证 | e2e 脚本（借 OpenPencil 仓库 Playwright + 系统 Chrome）：空态→保存→校验拦截→reload 持久化→月视图统计，全部通过，0 页面错误；脚本见迭代日志 08 |

## 已知留白（进 08 迭代清单）

- 记一笔一次只能选一个动作（多条明细是同一动作的重量组数）；练多个动作需返回再记一次——够用但可优化
- 月视图点阵无 aria 标签、无键盘操作
- 导入 JSON 未做（只有导出）
